# INET Cloud Environment Source Matrix

This file maps each required Vercel or bridge variable to its source. Do not paste real passwords, API keys, merchant keys, or service-role keys into this repository.

## What INET Already Provided

From the INET handoff:

- Web portal URL: INET portal for VM power/control.
- VM name: `SST-Innovation-01-POC`.
- VM OS: Windows Server 2025.
- Private IP: `172.24.4.123`.
- Public IP: `203.154.39.123`.
- Firewall policy includes HTTP `80` and HTTPS `443`.
- VM login credentials were provided out-of-band. Keep them out of GitHub and Vercel.

This is enough to host the payment bridge on the INET VM. For database cutover, this UAT project must either run a Supabase-compatible API on INET Cloud or be refactored away from `@supabase/supabase-js`.

## Vercel Variables

| Vercel variable | Source | Current action |
| --- | --- | --- |
| `POS_DATABASE_TARGET` | App safety config. | Use `inet_cloud` for this UAT project. This prevents accidental use of a Supabase-hosted POS Preview database. |
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase-compatible API URL hosted on INET Cloud, or a future refactored INET data API. | Do not point this UAT project at the POS Preview Supabase project. |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Public API key/JWT for the INET-hosted Supabase-compatible API. | Required only after the INET database API is provisioned. |
| `SUPABASE_SERVICE_ROLE_KEY` | Server-side service role/JWT for the INET-hosted Supabase-compatible API. | Server-only. Never expose to frontend or commit to GitHub. |
| `INET_PAYMENT_BRIDGE_URL` | Stable HTTPS URL of the bridge hosted on INET VM. | Create after bridge is installed on `203.154.39.123`; prefer a DNS name with TLS, for example `https://inet-bridge.example.com`. |
| `INET_PAYMENT_BRIDGE_API_KEY` | Shared secret generated by us for Vercel POS -> bridge calls. | Generate a new random value. Put the same value in Vercel and bridge `.env` as `BRIDGE_API_KEY`. |
| `INET_NOPS_ENV` | App config. | Use `uat`. |
| `INET_NOPS_AP_URL_UAT` | Public result page in the Vercel POS app. | `https://pos-inet-cloud-uat.vercel.app/payment/inet/result` |
| `INET_NOPS_CALLBACK_PUBLIC_URL` | Public callback URL in the Vercel POS app if INET calls Vercel directly. | `https://pos-inet-cloud-uat.vercel.app/api/payments/inet/callback` |

## Bridge Variables On INET VM

The bridge source lives at `apps/inet-payment-bridge` in this repository.

| Bridge variable | Source | Current action |
| --- | --- | --- |
| `PORT` | Bridge config. | Use `8787` internally, or `443` behind IIS/Nginx/Caddy reverse proxy. |
| `BRIDGE_API_KEY` | Generated by us. | Same value as Vercel `INET_PAYMENT_BRIDGE_API_KEY`. |
| `INET_API_BASE_URL` | INET UAT payment endpoint. | Use INET UAT API base URL already tested for QR creation. |
| `INET_MERCHANT_KEY` | INET Payment UAT merchant key. | Use only in bridge `.env`, never in Vercel frontend or GitHub. |
| `INET_AP_URL` | User return/result URL. | `https://pos-inet-cloud-uat.vercel.app/payment/inet/result` |
| `POS_INET_CALLBACK_URL` | POS callback endpoint that bridge forwards paid callbacks to. | `https://pos-inet-cloud-uat.vercel.app/api/payments/inet/callback` |
| `INET_CALLBACK_SECRET` | Shared callback verification secret. | Generate/store server-side only. Must match POS callback expectations if used. |
| `SUPABASE_URL` | Current Supabase URL, optional for phase 1 bridge updates. | Use only if bridge writes directly to Supabase; otherwise leave blank and forward callbacks to POS. |
| `SUPABASE_SERVICE_ROLE_KEY` | Current Supabase service role key, optional. | Prefer blank for phase 1 if callback is forwarded to POS. |

## INET Cloud Database Gap

The received INET details describe a VM, not a database service. For database cutover, request one of these from INET:

- Managed PostgreSQL endpoint: host, port, database name, username, password, SSL mode.
- Or permission to install PostgreSQL on VM `SST-Innovation-01-POC`.
- Confirmation whether Supabase Auth, PostgREST/Data API, Realtime, Storage, and RLS equivalents are required.

Important: the current app imports Supabase clients directly. A plain PostgreSQL `DATABASE_URL` alone will not replace `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, and `SUPABASE_SERVICE_ROLE_KEY` without additional backend refactoring. `POS_DATABASE_TARGET=inet_cloud` is used to fail closed if someone accidentally points this UAT deployment back to `*.supabase.co`.

## Recommended Next Step

1. Install and run `apps/inet-payment-bridge` on the INET VM.
2. Put a stable HTTPS reverse proxy in front of it.
3. Set Vercel `INET_PAYMENT_BRIDGE_URL` to that HTTPS URL.
4. Provision the INET-hosted database/API target before login or POS transaction testing.
5. After database target is confirmed, migrate schema and isolated UAT data.
